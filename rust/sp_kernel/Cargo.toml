[package]
name = "sp_kernel"
version = "0.1.0"
edition = "2021"

[profile.release]
lto = true
codegen-units = 1

[dependencies]
serde_json = { version = "1.0.151", features = ["preserve_order", "float_roundtrip"] }
